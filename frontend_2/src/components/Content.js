import React from 'react';

function Content() {
  return (
    <main style={contentStyle}>
      <p>Bem-vindo à ExchangeRate!</p> 
    </main>
  );
}

const contentStyle = {
  padding: '2em',
  textAlign: 'center',
};

export default Content;